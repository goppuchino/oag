version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

branches:
  docs:
    active: true

formats:
  - pdf
  - epub
  - htmlzip

sphinx:
  configuration: source/conf.py

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - source
